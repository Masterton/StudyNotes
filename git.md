# git 常用命令

```
git add . 把修改的文件提交到缓存区
git commit -m "备注" 将暂存区文件提交到版本库中
git branch dev 新建 dev 分支
git checkout dev 切换到 dev 分支
git checkout src/aa.txt 把指定文件的这次修改取消掉
git fetch upsteream 更新远程跟踪分支 upsteram
git remote add upstream http://aa 添加远程仓库
```